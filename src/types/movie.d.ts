export interface Movie {
  id: number;
  release: Date;
  title: string;
  // originalTitle?: string;
  // poster: string;
  // rating: number;
  // votesCount: number;
  // director: string;
  // genres: string;
}

export interface Movie {
  id: number;
  release: Date;
  title: string;
  rating: number;
  votesCount: number;
  // originalTitle?: string;
  // poster: string;
  // director: string;
  // genres: string;
}
